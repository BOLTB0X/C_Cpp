#include <iostream> //using 을 이용해 표준 라이브러리(std) 모두 사용하게 함
#include <string>
using namespace std;

int main(void) {
	string input;
	cin >> input;
	cout << input << endl;
	system("pause");
	return 0;
}